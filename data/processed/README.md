# Processed Data

This directory contains cleaned and transformed data in Parquet format.

Files are generated automatically by the ETL pipeline.

**Note:** Files in this directory are ignored by Git (see `.gitignore`)
