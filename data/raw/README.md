# Raw Data

Place your source data files here.

## Supported Formats
- CSV (`.csv`)
- Excel (`.xlsx`, `.xls`)
- Parquet (`.parquet`)

## Example
```bash
cp your_transport_data.csv data/raw/
```

**Note:** Files in this directory are ignored by Git (see `.gitignore`)
